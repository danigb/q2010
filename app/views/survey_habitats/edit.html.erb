<% title("Habitat") %>

<% form_for @survey do |form| %>
  <%#= next_step cuestionario_path(:id => 'presentacion') %>
  <%= save_or_preview_survey(form) %>
  <%= render 'habitat', :form => form %>
  <%= save_survey(form) %>
<% end %>