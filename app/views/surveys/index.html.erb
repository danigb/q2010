<%= title 'Cuestionarios' %>
<br/>
<div class="preview">
  <p>Enhorabuena! Tu cuestionario está completo.</p>
</div>
<div class="about">
  <p>Puesto que has completado tu cuestionario, ahora puedes ver las respuestas de todos los participantes que han completado su cuestionario.<br/>
    También puedes <%= link_to 'seguir editando', view_survey_presentacion_path %> tu cuestionario</p>

  <table>
    <thead>
      <tr>
        <td>Participante</td>
        <td>Cuestionario</td>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.username %></td>
          <td>
            <% Survey::CUESTIONARIOS.each do |name| %>
              <%= link_to name, {:controller => "survey_#{name}s", :action => :show, :id => user.id} %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
